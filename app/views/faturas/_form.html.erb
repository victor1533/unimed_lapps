<%= form_for(fatura) do |f| %>
  <% if fatura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fatura.errors.count, "error") %> prohibited this fatura from being saved:</h2>

      <ul>
      <% fatura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group form-material">
    <div class="field">
      <%= f.label :valor_total %>
      <%= f.text_field :valor_total, :class => "form-control" %>
    </div>

      <div class="field">
      <%= f.label :data_emissao %>
      <%= f.text_field :data_emissao, :class => "form-control" %>
    </div>

      <div class="field">
      <%= f.label :data_vencimento %>
      <%= f.text_field :data_vencimento, :class => "form-control" %>
    </div>

    <div class="actions" align="center">
      <%= f.submit %>
    </div>
  </div>
<% end %>
